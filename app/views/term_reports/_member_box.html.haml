- # locals:
- group_name || raise(ActionController::ParameterMissing, 'no group_name given')
- members ||= term_report.members(group_name)

.card
  .card-header
    %h3
      = group_name
      = "(#{members.count})"
  .card-body
    - members.each do |user|
      %li= link_to user.title, user
