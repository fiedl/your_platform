
$('.new_users_waiting i:last').remove();

$('.members_count').text("(<%=j @user_group_membership.group.members.count.to_s %>)");

new_list_item = $("<%=j render partial: 'user_group_memberships/list_item', locals: {user_group_membership: @user_group_membership} %>");
new_list_item.appendTo('ul.new_memberships').hide().show('scale', {}, 900);

$('.new_users_please_reload').removeClass('hidden').show()