namespace :your_platform do
  namespace :install do

    task :node_modules do
      your_platform_path = File.expand_path(File.join(__FILE__, "../../.."))
      print 'Running `yarn install` from ' + your_platform_path + "\n"
      `cd #{your_platform_path}; yarn install`
      print "Done running yarn_install.\n\n"
    end
  end
end
