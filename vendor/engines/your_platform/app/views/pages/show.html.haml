= content_box(:heading => best_in_place(@page, :title)) do
  - best_in_place @page, :content, type: 'textarea', sanitize: false

= html_convert_h1_to_boxes( render(partial: 'pictures') + render(partial: 'videos') + render(partial: 'attachments') )

%div.attachment_global_drop_zone
  = t :drop_files_here



