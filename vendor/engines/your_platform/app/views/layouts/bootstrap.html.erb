<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Wingolfsplattform" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= stylesheet_link_tag "bootstrap_layout", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>

    <% if user_account_signed_in? %>
    <div id="logged-in-bar" class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
	  <!--//
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a> //-->
          <a class="brand" href="#">Wingolfsplattform</a>
          <!--div class="container-fluid nav-collapse"-->
            <ul class="nav">
              <li><%= link_to t( :my_profile ), main_app.user_path( current_user_account.user ) %> </li>

              <li class="dropdown">
		<%= link_to( 
		    ( t( :my_groups ) + " " + content_tag( :b, '', class: 'caret' ) ).html_safe, 
		    main_app.my_groups_path, 
		    class: 'dropdown-toggle', data: { toggle: 'dropdown' } ) %>
		<ul class="dropdown-menu">
		  <%= my_groups_table %>
		</ul>
              </li>

              <li class="dropdown">
		<%= link_to( 
		    ( "Favoriten" + " " + content_tag( :b, '', class: 'caret' ) ).html_safe, 
		    main_app.my_favorites_path, 
		    class: 'dropdown-toggle', data: { toggle: 'dropdown' } ) %>
		<ul class="dropdown-menu my-favorites">
		  <%= bookmarked_objects_lis( current_user ) %>
		</ul>
              </li>


	      <li class="last dropdown">
		<%= render "layouts/session_bar_avatar" %>
		<a class="dropdown-toggle" data-toggle="dropdown" href="#">
		  <%= current_user.name %>
		  <b class="caret"></b>
		</a>
		<ul class="dropdown-menu">
		  <li><%= link_to t( :my_profile ), main_app.user_path( current_user ) %> </li>
		  <li><%= link_to t( :logout ), main_app.destroy_user_account_session_url, method: :delete %></li>
		</ul>
	      </li>
            </ul>
          <!--/div--><!--/.nav-collapse -->
        </div>
      </div>
    </div>
    <% end %>

    <div class="container-fluid">
      <span id="logo">
	<%= image_tag 'logo.png' %>
      </span>
      <span id="search_box">
	<%= render partial: 'shared/search_box' %>
      </span>
      <span id="category_indicator">
	<ul class="nav nav-tabs">
	  <%= backend_horizontal_nav_lis %>	
	</ul>
      </span>
      
      <ul><!-- class="breadcrumb" -->
        <% if user_account_signed_in? %>
	<span id="star_tool">
	  <%= star_tool( current_user, @navable ) %>
	</span>
          <% end %>
	<span id="breadcrumb">
	  <%= breadcrumb_ul %>
	</span>
      </ul>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span3">
          <!--li class="nav-header">Sidebar</li-->
	  <%= vertical_menu %>
        </div><!--/span-->
        <div class="span9">

          <% if flash[ :error ] %>
            <div class="alert alert-error">
	      <button class="close" data-dismiss="alert">×</button>
	      <%=h flash[ :error ] %>
	    </div>
          <% end %>
          <% if flash[ :notice ] %>
            <div class="alert alert-success">
	      <button class="close" data-dismiss="alert">×</button>
	      <%=h flash[ :notice ] %>
	    </div>
          <% end %>

          <%= html_convert_h1_to_boxes( yield ) %>
        </div>
      </div><!--/row-->

      <footer>
        <!--p>&copy; Company 2012</p-->
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <%= yield :scripts %>


  </body>
</html>
